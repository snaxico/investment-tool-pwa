{
  "name": "investment-tool-pwa",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && node --check sw.js && node --test tests/*.test.mjs"
  }
}
